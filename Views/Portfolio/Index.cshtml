
@{
    Layout = null;
}

<!--
Author: W3layouts
Author URL: http://w3layouts.com
-->
<!doctype html>
<html lang="en">

@Html.Partial("PartialHead");

<body>
    <!-- header -->
    @Html.Partial("PartialNavbar");
    <!-- //header -->
    <!-- banner section -->
    @Html.Action("PartialFeature");
    <!-- //banner section -->
    <!-- home page about section -->
    @Html.Action("PartialAbout")
    <!-- //home page about section -->
    <!-- home page second section -->
    @Html.Action("PartialMyResume");
    <!-- //home page second section -->
    <!-- home page services section -->
    @Html.Action("PartialService");
    @Html.Action("PartialProject");
    <!-- //home page services section -->
    <!-- stats -->
    @Html.Action("PartialStatistics");
    <!-- //stats -->
    <!-- testimonials -->
    @Html.Action("PartialReference");
    <!-- //testimonials -->
    <!-- home page video popup section -->
    @Html.Action("PartialVideoPopUp")
    <!-- //home page video popup section -->
    <!-- freelance hire me -->
    @Html.Action("PartialHireMe")
    <!-- //freelance hire me -->
    <!-- Footer -->
    @Html.Action("PartialFooter");
    <!-- //Footer -->
    <!-- all js scripts and files here -->

    @Html.Action("PartialScripts")

</body>

</html>